console.log("Hello there");

// lvl_1_2
/*
const example = document.querySelectorAll('.example');

const myFunction = () => {
    example.forEach(element => {
        element = element.style.backgroundColor = "black";
    });
}
*/

// lvl_1_5
/* 
    let count = document.getElementById('clickMe');
    let anzahl = 0;

    count.addEventListener('click', countOne = () => {
    anzahl ++
    count.innerHTML = "Click me: " + anzahl
});
*/

// lvl_1_6
/*
const selectElement = document.querySelector('#my-select');

selectElement.addEventListener("change", (event) => {
    const result = document.querySelector('#option-selected');
    result.textContent = `Sie haben ausgewählt ${event.target.value}`;
});
*/